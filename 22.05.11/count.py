#Viết một chương trình chấp nhận đầu vào là một câu, đếm chữ hoa, chữ thường.
#hơi khó
x = input('Enter your sentence here: ')

def count(sentence):
values{upper, lower} = 0

for l in x:
	if l.isupper():
		 values[
